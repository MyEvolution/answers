#include<iostream>
#include<vector>
#include<string>
using std::vector;using std::cin;
using std::string;
int main()
{
	int i;
	vector<int> bag;
	while(cin>>i)
	bag.push_back(i);
	/*string line;
	vector<string> bag2;
	while(cin>>line)
	bag2.push_back(line);*/
	
	return 0;
}
